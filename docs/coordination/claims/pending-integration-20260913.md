# Pending integration and local commits, 2026-09-13

- User now authorizes integration and commits; no push, cloud deployment, installed update, app termination or cross-task dispatch.
- Target: current public/private integration/source-unified-20260909. Preserve every pre-existing change; snapshot hashes and explicitly resolve only authored candidate deltas against their frozen baselines. Do not copy entire older worktrees or deployment artifacts.
- Scope: DST compiler/failure-outcome candidate (5 modules/3 tests), iFlytek streaming candidate (4 public/6 private modules plus tests), private runtime diagnostic retention and integration evidence. Existing integrated work may be included in commits only after scope confirmation and review; generated binaries, credentials, customer data and private routing never enter public source.
- Keep: strict sandbox/capability restrictions, successful real trial + verification before skill persistence; no butterfly-specific atom; current sword power/HUD/clockwise behavior and compatibility. Missing outcome is not failed gameplay.
- Keep reply-first: normal Agent text and actual playback completion precede ordinary game operation; Work keeps its existing text-completion recognition boundary; emergency stop remains immediate. ASR partials are display-only, never Agent input. No late/cancelled callback, silent full-recording replay, model/provider/voice switch, pricing change, account/save/config mutation.
- Log proposal accepted for implementation: runtime diagnostic family only, 7 days / 100 MiB total / 10 files including active / 10 MiB per file. Exclude sessions, skills, saves, ledgers and all unrelated/symlink targets. Disk errors cannot crash Desktop. Do not delete installed historical logs in this turn. No new promise of complete redaction without evidence.
- Before implementation: run imported behavioral regressions against current old production sources (or isolated copies), capture assertion failures, then same regressions on integration. Test actual compiler/worker/store/registered tools, transport/HTTP bridge/controller, and retention filesystem; mock only external services/game/media. Record baseline failures separately.
- Validate plugin/Work/integration/platform, private check and secret scan, native sword regression if committing its pre-existing changes, diff hygiene, source-only changes and staged file inventory. No CI/packaging hard-gate or real mic/game/installed validation claim.
- Commit plan: local commits on integration branches, explicit reviewed paths, public/private isolated; do not stage secrets, generated artifacts or unrelated unreviewed changes. Shared manifests remain unchanged unless specifically required and reviewed for integration.
- Current authorization (2026-09-13 follow-up): user explicitly requests updating the installed application, committing the complete integrated changes, and merging into local main in both repositories. This supersedes the earlier pending-scope/no-install restrictions above. No remote push, cloud deployment, provider/config changes, or cross-task dispatch. Preserve dirty main work before merging; inspect origin/main so published changes survive. Update the private Core pin to the verified new public commit for reproducible builds. Check app/game closure again before installation; no forced process termination is authorized.
- Verification already completed: public/Work/workspace/private checks, secret scan and explicit native bridge tests; see docs/testing/PENDING_INTEGRATION_20260913.md. Recheck any new merge differences and the exact installed candidate. Other claims and shared STATUS remain untouched.
