# 三款游戏辅助功能实施与验收

本文件记录 2026-09-07 的独立工作区改动，不代表已安装或真实游戏通过。
源码：`C:/game/ai-native-game-harness-worktrees/player-help-20260907`。
基线：06a870b + 20260907 已封存 public candidate 的 447 个源码文件。
基线内已有的改动不是本轮新功能。未提交、未推送、未覆盖安装版。

## 本轮代码

| 游戏 | 新增实现 | 验收边界 |
| --- | --- | --- |
| 星露谷 | 浇水、收获支持 location/x/y/width/height 矩形；最多 1024 格 | 未提供圈选界面；“这片地”不明确必须询问，不能猜坐标；保留原空参数全地图行为 |
| 星露谷 | `stardew.inspect_planning` 查询 NPC 当前已加载位置、完整背包、今日状态和任务 | 只读事实；不等于跨地图导航、献祭建议或明日天气预测 |
| 饥荒 | `dst.inspect_companion` 读取真实容器数量、位置、健康与附近正在攻击的实体 | 不生成新同伴；不是所有环境危险检测 |
| 饥荒 | `dst.move_to` 有界、可取消移动；`dst.pick_target` 原生采摘；寻找支持可采过滤 | 采摘产物仍需另行拾取；不能用拾取地面草冒充采草丛 |
| 饥荒 | TS `xiaotangyuan_gather_resources`：数量目标、库存增量检查、攻击/掉血中止、返回核验 | 1–50 个、最多 50 次、240 秒；仅附近；返回失败明确报告；取消后不另起返回动作 |
| 缺氧 | `oni_inspect_selected` 读取选中对象状态、运行条件、温度并在 TS 解释 | 新旧入口都注册；拒绝过期数据；不臆测整条电网或管网拓扑 |

任务规则位于 TS；Mod 只提供事实和原子动作。新增代码没有 Python 运行依赖。
饥荒采集工具是内置任务编排，不是“已学习并保存的新技能”。学习工具可以看到新增原子，但其生成、试跑、保存仍须独立验收。

## 第二批代码（本次继续实施）

| 游戏 | 新增实现 | 仍需注意 |
| --- | --- | --- |
| 星露谷 | 献祭原始数据+完成槽位+普通交物任务联查，TS 留物建议 | 支持数量、品质、类别与可选献祭已完成；不自动卖出；不含特别订单和未来用途 |
| 星露谷 | 读取明日天气、工具升级状态，下矿镐子/武器/食物检查 | 水壶建议仍要求确认取回日浇水和铁匠营业，不保证升级窗口 |
| 星露谷 | 根据已加载出口图给出 NPC 跨地图路线提示 | 门禁、时间、动态障碍未验通；不会传送或自动行走 |
| 饥荒 | 读取真实配方材料并计算缺口；出门/临冬补给检查 | 不等于科技已解锁；当前不统计背包容器、基地箱子或所有 Mod 装备 |
| 饥荒 | 服务端采集 death/attacked 事件，区分最终死因与此前攻击 | 保存本次登录最近死亡及前 120 秒内至多 20 次攻击；重启前历史不在范围内 |
| 缺氧 | 关联电路实际负载/需求/发电/储电/安全容量诊断 | 区分已超安全负载与全部启动的潜在风险 |
| 缺氧 | 输入/输出接口、管网端点和至多 256 格管道事实 | 缺少接收端可定位到连接网络；不把满管直接解释成某格损坏 |
| 缺氧 | 当前世界氧气/食物库存净变化与条件耗尽估计 | 至少两次查询、跨度 0.1 周期；世界/存档/人口/可见范围变化重置；不是精确生产消耗账本 |

新工具仅在当前游戏声明对应只读原子后注册。缺氧两项检查均测试过文件协议请求/结果往返；星露谷、饥荒测试过工具入口到原子调用，不只是独立算法。

## 仍未完整覆盖的原需求

- 星露谷：玩家友好的区域圈选；箱子/特别订单联查；门锁和格子级通行校验；上次目标的可靠恢复和主动提醒。
- 饥荒：远距离搜索与避险路径；背包容器和特殊装备识别；跨重启死亡历史；所有环境危险识别；真实学习—保存—再次执行闭环验收。
- 缺氧：精确阻塞格与桥接方向归因；任务优先级/权限/可达性联查；可用食物/可达氧气的精细生产消耗预测；完整基地规划；按条件主动汇报。
- 三款游戏：更新发行清单、安装包集成及真实麦克风/气泡/取消/任务完成通知验收。本轮没有部署，所以当前安装版不会自动拥有以上源码改动。

## 已运行验证

- 星露谷 C# Release 编译：0 错误、0 警告，EnableModDeploy=false。
- FieldScope 独立 .NET 8 测试：21 条断言通过；边界、缺字段、错误地图、非整数、超面积和 JSON 传参。
- 第一批小汤圆 TS 编译与回归：26 文件 / 192 测试通过，包含 7 个新增采集任务测试；第二批最终结果见下。
- 第二批小汤圆 TS 编译与回归：28 文件 / 207 测试通过，新增留物、路线、下矿、配方、死亡复盘和工具注册/调用检查。
- 第二批缺氧 TS 编译与回归：4 文件 / 30 测试通过，包含诊断、趋势与两项检查的协议往返。
- 缺氧 C# Bridge Release 编译通过；保留游戏引用的两条程序集版本警告。
- DST Mod Lua 语法检查通过。未执行真实游戏 Lua 行为验收。

## 接下来的真实验收

部署必须成套更新插件与对应 Mod/Bridge，不能只更新其中之一。请在游戏和应用正常退出后再安排安装。

1. 星露谷：准备两块田，明确指定矩形；只有矩形内改变。换地图后重放旧请求必须拒绝；成熟与未成熟作物混种时，只收成熟作物。
2. 星露谷：查 Haley/海莉；返回实际当前位置。没找到不得编造日程；背包查询不应遗漏第 13 个以后的物品。
3. 饥荒：容器预放 7 个草，再要求采 20 个；最终核对新增 20 个，并确认返回。随后测试满容器、无草丛、攻击者、路径不通和语音取消。
4. 缺氧：点选缺电水泵，读取实际状态；再取消选择，必须提示先选择。读不到整条管网时不得指认某格堵塞。

模拟测试成功只说明协议与规则受测，不说明真实游戏动作、麦克风、UI 或玩家体验已成功。
