# 剑阵当前强力档（当前任务续作）

- 用户本轮要求：当前数值调高，升级系统以后再做。取代上一轮“不改变捕鱼/打怪数值”的限制，仅本轮数值范围变化。
- 目标：每剑基础伤害8→40；默认8崽，砍树最多4→8棵、每崽最多12→24次原生斧击；捕鱼最多3→8次原生抽取，不保证8条鱼。
- 工作区：source-unified-20260909，当前任务此前整合的脏工作区继续；不提交、推送、跨任务调度或新建子代理。
- 允许：ProjectileGroup 的有界预算/参数声明/伤害；sword-formation TS 编排及 Gateway/SkillTools 的真实能力协商；相应原生/TS测试、当前认领和报告、本轮构建与安装脚本。
- 保持：1点体力、8秒冷却、60秒召唤期限、4正4背/单剑/顺时针、实际原生伤害/掉落计数、6格资源范围、现有斧头、果树/树液器保护、取消立即生效、HUD/当前状态与历史隔离。不引入升级、自动学习、清树桩或自动拾取。
- 兼容：旧Mod仅收到其声明的旧预算；捕鱼新可选预算省略时仍是3次。不得用仅存在一个参数判断可发送更高上限。
- 时序：实际Agent正常回答与播放完成后才作业；停止不等正常回复；Work、普通聊天、学习和其他游戏接线不改变。
- 先冻结已安装整合版本对应源码，在旧版运行新的真实倒树/伤害/捕鱼目标断言，记录数值失败；再改实现，同一用例通过。真实TS→原生ProjectileGroup→SwordResourceTargets，模拟外部游戏世界/模型，不模拟待测边界。
- 验证：原生视觉/控制器/作用范围；TS剑阵和生产Agent/Gateway、状态、取消、语音/Work相关联合回归；TS和Mod构建；产物及安装两副本/恢复包哈希。
- 部署：当前应用正在运行，须新退出窗口；不强杀进程。不修改 app.asar、供应商/Key/账号/存档、其他Mod、共享打包热点。安装脚本如发现漂移应暂停。
- 完成边界：源码/自动回归/候选/已安装/真实游戏分别报告。原生套件显式启用，尚无CI/打包硬门禁。当前状态：实现前冻结与反证准备。

## 当前证据

- 冻结旧实现三项数值目标断言失败；候选185项联合回归、原生33/229/21通过，TS与Mod编译通过。
- 34目标候选就绪，详见 `docs/testing/SWORD_POWER_20260913.md` 和 output/sword-power-20260913/stage-1vQ7f9/manifest.json。
- 用户确认退出后的首次安装被仍存活的原5进程阻止，未覆盖任何文件；后续启动检查唤起旧窗口，已如实说明。等待正常关闭许可，不擅自强杀。

## 续作：闲置文字不常驻

- 用户截图明确要求“剑阵未展开 · 可用”不一直显示。仅改变HUD显示条件：首次进入/完全闲置/冷却结束后无常驻条；环绕、出击、收回和未结束的冷却仍显示。
- 不清空真实状态：观察与回执保留ready、screenLabel、hudVisible=false，AI仍可正确判断可用，不改计时/语音气泡/急停/伤害参数。
- 允许补充ProjectileGroup与真实原生绘制/观察测试，以及本轮报告/产物；其他生产代码和共享热点不变。
- 先冻结强力档源，新增完整Draw→CaptureLiveState用例在旧实现失败，再候选通过。保留上轮185项相关联合回归，更新待装清单，旧stage不再作为最新候选。
- 部署仍需新的退出窗口：用户此次截图不是关闭游戏/应用授权，不能沿用已消费的退出许可。

### 续作交付进展

- 新增四条真实绘制/观察回归先旧失败再新通过；37原生、229控制器、21范围、185相关TS回归通过，Mod/TS编译成功。
- 本轮稍后只读检查确认游戏与Harness已经退出，因此完成此前用户请求的待办增量安装；未调用关闭或强杀操作。
- 最新34目标stage-yDShza已安装并验证保护文件未改；代替旧power未安装stage，详情SWORD_IDLE_HUD_20260913.md。新主进程23236用于启动验收，游戏未启动，正在核对服务。

- 最终复核：34目标和恢复归档匹配，Web49856返回200，33145归属Runtime82208（主进程23236），Web/Gateway/product bridge均就绪、无启动fatal。源码/自动回归/本机增量安装完成；真人语音和真实游戏画面未验收。应用有意保持运行，无本任务遗留测试进程。
