# 待集成补丁合并验收（2026-09-13）

## 范围与交付边界

本轮将饥荒学习候选和讯飞流式识别候选的**作者增量**合入统一源码，未复制旧 worktree 的整份差异、旧安装归档或生成物。私有日志策略在私有仓库实现。没有修改模型/音色、代理、账户、定价、存档、已安装 Runtime/Profile/Mod；没有启动游戏、关闭应用或跨任务调度。

用户要求“合并并提交”。待提交范围检查发现公共 230、私有 113 个文件（本报告加入前的快照），包含大量本轮之前已整合但未提交的成果。已询问是否一并提交；尚未收到范围选择，当前未暂存、未提交、未合并 main、未推送。新补丁与这些既有未提交代码存在依赖，不能将某几个改动文件直接提交并称为完整可构建版本。

## 合并决定

- 学习：支持 `===`/`!==` 的词法别名但保留严格比较、字符串/注释、能力和预算限制；区分编译/准备/执行/验收失败；没有结果不伪装成试跑失败。仍只在真实执行结果与独立验收都成功后持久保存，不新增蝴蝶专用原子。
- ASR：公共四个、私有六个生产模块；8 个文本冲突逐一处理。保留后来增加的错误分类、分段校验、取消和 TTS 保护，加入按音频时间发送、部分结果 SSE、最终结果截止和短录音初始化复用。
- 旧测试允许某些断网自动整段重传，与流式候选“不静默重传”的约定冲突。本轮采用后者；玩家需重新录音，不重复上传已有完整录音。错误代码仍保留用于人话提示。
- 部分识别只用于状态显示，最终文本才能进入 Agent。没有改变普通游戏回复及实际播放完成屏障、Work 现有识别边界或急停。字幕已传到 Gateway 不等于真实游戏界面已显示。
- 日志：运行诊断按7天、总100 MiB、含当前文件最多10份、每份10 MiB保留；包含 fallback，旧大文件/过期文件在运行日志写入清扫时处理。仅精确匹配本日志族的普通文件，拒绝链接/目录；磁盘或清理失败不能令 Desktop 崩溃。锁定文件无法删除时只能告警，不承诺无条件硬限额。不修改已安装日志、聊天、技能、存档或账本，也不宣称本轮完成了所有日志内容脱敏。

## 反证

证据目录：`C:/game/deepseekharness/output/pending-integration-20260913`。

| 当前旧实现 | 结果与失败原因 |
| --- | --- |
| 新学习与公共ASR用例 | 12通过、17失败：学习15项，公共ASR部分回调/取消后旧completed两项 |
| 私有传输与真实HTTP桥 | 1通过、6失败：部分结果、末帧/end时机、定时漂移、本地与托管双向流 |
| 日志新策略 | 4项目标断言失败：过期未清理、数量/总量未限制、超大写入与fallback无上限 |
| SSE与现有错误分类组合 | 冻结旧扩展不能通过部分/最终流和SSE额度错误用例，返回ASR_PROTOCOL；新扩展通过 |

旧私有用例最初还将新的结构化 `ASR_CANCELLED` 与旧英文错误文本比较；已修正后重新跑旧版，不将该文字差异算作目标失败。候选最初技能子进程仍加载旧 dist，3项失败；执行必需的真实编译后同一测试通过，没有绕过或模拟子进程。

## 候选验证

- `pnpm --filter @qimidandapigu/dsh-xiaotangyuan-game run check`：373通过、41默认跳过。
- 显式原生 TS→C# / Agent/Gateway/播放屏障 / 旧Mod兼容套件：47通过、0跳过（与常规测试有重叠，不相加宣称独立总数）。原生绘制37通过。
- 学习/公共ASR针对性：39通过；私有ASR/HTTP桥/分段校验：22通过。
- Work `check`：44通过；`pnpm test`：集成64、平台22通过。
- `pnpm run build`：统一工作区构建通过。
- 私有 `pnpm check` 与 `scan:secrets`：通过；最终详细数目见私有报告及 `private-check-final.log`。根测试有1项Windows文件符号链接权限跳过。
- 公共待提交扫描两个命中均逐项确认为测试占位：`sk-dummy-test-credential` 与 `local-smoke-only`。没有发现真实密钥、禁止凭据文件或待提交生成二进制。私有检查无命中。
- 两边 `git diff --check` 通过。测试输出和合并前逐文件快照只放证据目录，不提交生产数据。

模拟仅限外部模型、游戏世界/GPU、语音服务、凭据fixture、Media Host等。待测编译器/子进程/工具注册/持久化/HTTP桥/公共Controller及文件系统保留逻辑是真实生产实现。未做真实供应商延迟、真人麦克风、游戏长期存档、安装器、云部署或退出→进程归零新验收。原生套件仍需显式开启，未建立新的CI/安装硬门禁。

私有发行清单仍固定旧核心版本；本轮不制作安装包。后续确认提交范围并生成核心提交后，再显式固定发行版本和进行打包/安装验收，不能拿旧标签替代新源码。

## 后续授权：提交、合并与本机更新（2026-09-13）

用户随后明确要求更新现有安装、提交并合并到 main；本节取代上文的待确认/未安装状态，保留之前的测试历史。

- 完整公共整合提交 `0db906b`；保留远端 README、截图及 FromNextWorld 归属更新后，`d0e213a` 已合入本地 main。没有推送。仅本地构建标签 `desktop-integrated-20260913` 固定该核心提交。
- 从该提交执行 `pnpm desktop:prepare`，使用本机已有 .NET SDK；源码封存标识 `ef14146711102445b6d76fa717dd674a8061bb95fca8c43572ebc465889a3ffc`，构建/归档一致性核验通过。
- 合并后显式启用 C# 桥接：小汤圆 check 414/414；集成64/64、平台22/22。没有用默认跳过项冒充原生通过。
- 对现有安装增量更新129个文件（包含私有组合、Runtime、两个 Profile、恢复归档和饥荒安装资源）；408个无关文件保持原哈希。启动后6份实际插件目录、816个文件与源码构建一致，Gateway ping 返回 pong，Web、产品桥和媒体组件均就绪。
- 组合后的真实 Electron 退出处理器在4种隔离场景通过，子进程剩余0；这是隔离退出测试，不是对本次正在运行的应用执行退出验收。当前保留一个正常应用实例供用户测试。
- 日志策略启动后清理旧诊断日志约2GB；没有另备份这些旧日志。聊天、技能、设置和游戏存档不在清理范围。
- 安装时额外备份目标文件。最初外层核验将 pnpm 目录别名误列为未改文件；按实际路径及声明目标的新哈希核实23个别名后通过，未放宽无关文件保护。
- 证据：任务入口 `output/pending-integration-20260913/release-*.log`、`active-release-verification.json`；安装清单与备份位于私有整合树 `.artifacts/local-update-hrbEsw/`。
- 未进行真人麦克风、真实模型费用/延迟、真实游戏动作、云端部署、新安装器或macOS验收；没有新增CI硬门禁。
