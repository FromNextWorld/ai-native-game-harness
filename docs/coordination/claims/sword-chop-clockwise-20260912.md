# 剑阵召唤后砍树与顺时针环绕（当前任务）

- 工作区：source-unified-20260909；不提交、不推送、不跨任务调度。
- 用户要求：修复召唤后口头答应砍树但不执行；整体顺时针旋转。此前固定槽位的旧视觉规格被本次旋转要求取代，默认 10 秒一圈。
- 实测旧错误：runtime.log 2026-09-12T08:41:35.742Z，game.sword-formation.execute，requestId 56699d98-0e73-4607-af4c-c1f6be47cf15，已有投射物组，请先停止；随后 adapter.action.rejected atom=sword:chop。游戏后来是手动退出，不判为崩溃。
- 保持：四正四背、单剑、原素材、身体直立、等距椭圆；出击仍追实际目标及原生碰撞。普通陪聊/执行/学习/Work 首轮真实回复及音频播放完成后才判断操作，收回/取消保留紧急语义；不修改供应商、配置、存档、语音补丁或其他游戏。
- 拟改：现有原子增加可选状态读取和预览组 UUID 条件交接；TS 经实际 Adapter 能力声明开启。仅未发射、无资源副作用的 preview 可接续；先验斧头/目标/体力等，失败保持原组；拒绝覆盖工作组、过期/变更组。交接不得绕过取消墓碑、重复计费或延长原组 60 秒生命周期。
- 入口：Gateway post-reply 与已注册 sword 工具 -> TS 编排 -> RPC/主线程 -> 原生 ProjectileGroup -> 原生资源效果/真实回执；旋转覆盖实际 Geometry/Group/Renderer。检查动作失败通知是否能到玩家。
- 验收：修改前冻结源；新行为测试先对旧实现运行并因无作业成果/无旋转失败，再候选通过；使用真实生产控制器/绘制/编排，外部游戏和模型允许替身，不以编译失败为反证。
- 部署：本轮初始 Harness 仍运行，游戏已关闭；先构建核验，不覆盖运行文件。更新需新的退出窗口。核对实际加载的 runtime/profile/archive/Mod 及前轮语音修复，记录源码与产物哈希。没有真实游戏/麦克风验收不能说已实机通过。
- CI：本轮专项回归尚未接为发行硬门禁，不声称已有自动阻断。

状态：2026-09-12 17:27 完成代码、反证、候选编译和增量安装；真实游戏待用户验收。

结果与证据：

- 旧视觉反证25通过1失败；旧TS→原生连续作业首批1通过3失败（已有投射物组）；旧WebSocket动作结果未到Presentation的行为断言失败。
- 候选121项相关TS测试、26项真实绘制调用、229项原生/RPC检查、21项田块范围通过；Mod Release 0警告0错误。没有用编译失败冒充反证。
- 新原生接续覆盖普通召唤后砍树、先验失败保留原组、取消/过期/新组CAS、工作组拒绝覆盖；实际注册Skill工具及真实Agent→播放完成→Gateway→原生控制器均验证。外部模型/游戏为替身；音频门控验证不等于真人听感验收。
- 用户确认退出后仍有5进程，安装脚本在写入前阻止；用户随后允许结束进程，但再次读取时已自行退出，不需要强杀。34目标文件更新完成并全部哈希一致。
- Runtime43636（父74812）、Web57742、Gateway33145正常，新tgz为2d02b348…，活动Profile恢复依赖一致；星露谷未运行。原SpeechController/供应商扩展/app.asar/Mod配置及其他文件保留。
- 报告：docs/testing/SWORD_CHOP_CLOCKWISE_20260912.md。证据根：C:/game/deepseekharness/output/sword-chop-clockwise-20260912；manifest/installed/runtime-verification位于stage-HJOClL。
- 未提交、推送、制作新正式安装器或覆盖其他 dormant Profile；没有更新CI/发行硬门禁。真实麦克风、真实斧击和显卡画面仍需验收。
