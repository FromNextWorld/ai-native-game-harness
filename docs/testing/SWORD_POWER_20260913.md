# 剑阵强力档：数值、兼容与验收

> 后续状态：强力档已与闲置文字隐藏合并并增量安装，最新证据见 `SWORD_IDLE_HUD_20260913.md`。下文stage-1vQ7f9及“未安装”是首次阻塞记录，不再代表最新状态。

## 本轮数值

| 项目 | 上轮已安装整合版 | 本轮候选 |
| --- | --- | --- |
| 打怪 | 每剑基础8 | 每剑基础40，默认8剑基础合计320 |
| 砍树 | 最多4目标、每崽12次原生斧击 | 最多8目标、每崽24次，8崽总预算192次 |
| 捕鱼 | 最多3次原生抽取 | 最多8次原生抽取，不保证8条鱼 |

升级系统暂不加入。实际伤害受原版碰撞、护甲/无敌、存活血量和范围内目标影响；原生重叠命中可同时伤及多只怪，320不是全场伤害硬上限。砍树仍使用背包斧头，保留树桩/原版掉落、6格范围、果树/树液器保护。捕鱼不改原版抽取和传说鱼过滤，不保证出鱼。体力仍每作业1点、冷却8秒，继承原60秒召唤期限。

## 实现与兼容

- Native ProjectileGroup 声明并强校验新的有限上限；捕鱼可选 fishRollLimit 省略时仍3次。
- TS根据真实Adapter参数范围协商强力档；旧Mod继续12次/4目标/3次捕鱼，不因新TS发送未知参数而失败。
- Gateway后置操作与SkillTools注册均读取同一能力元数据。真实正常回复与播放完成后才判断、执行；急停保持立即执行。
- 新HUD、停止反馈、当前/历史状态隔离、四正四背单剑顺时针、爷爷素材、退出修复未改变。

## 回归证据

证据目录：`C:/game/deepseekharness/output/sword-power-20260913`。

- `snapshot.ps1`：冻结本轮前的Combat与TS源码；已安装插件3fc28d01...、DLL4ac7669b...与上轮整合匹配。
- `test-old.ps1` / `old-power.json`：冻结旧原生代码编译成功；三项目标断言均失败（4棵而非8棵、3次而非8次、64点而非320点）。不是依赖或编译失败。
- `test-integrated.ps1` / `final-tests.json`：185通过、0失败、0跳过，包含真实Agent/Gateway播放屏障后的砍树、捕鱼、打怪，真实TS→原生资源/伤害、旧Mod兼容、有界高血量失败、取消、受保护树、非法参数、当前状态/旧会话、语音/Work相关回归。
- 原生视觉33、控制器229（含真实回环WebSocket）、作用范围21通过；TS与Mod编译通过，Mod0警告0错误。
- 真实外部游戏世界/GPU、模型和部分语音为测试替身；未做真实麦克风或真实存档玩法验收。原生测试需要显式启用，尚无CI/正式打包硬性阻断。

## 候选与安装状态

- `stage-1vQ7f9/manifest.json`：34更新目标，其他编译JS/归档及保护文件无改动；原生源相比上一轮仅ProjectileGroup改变。
- 候选插件SHA256：`683e528191b5e0aabec1bf557ca91ed8c5fc7fa072e675036f6dceaec78ed2ca`。
- 候选DLL SHA256：`f8fb0889c6476df04ee9c02fd33417abeb0d6ccee401698a70a31127d3ac73e9`。内嵌爷爷图集与源文件哈希一致。
- 初次安装尝试被进程保护拦截，未覆盖文件：原主进程82628及4个子进程仍在。后续启动检查不慎唤起旧窗口（短暂进程7344已退出）；不能据此宣称新版已安装或窗口此前已完全退出。已向用户说明并请求正常关闭窗口后重试；不强杀进程。
- 当前待新的正常退出确认，尚无本轮installed.json。未修改共享打包热点、账户、Key、供应商、存档，未提交/推送或制作完整安装器。
