# 砍树强度与捕鱼/打怪接续核验

- 当前任务续作；source-unified-20260909，禁止跨任务调度、不提交/推送。此前剑阵接续与顺时针补丁已安装，作为本轮基线。
- 用户要求：解释旧原因、核验捕鱼/打怪；提高砍树能力，一次多砍倒几棵。默认方案为一轮最多4棵普通成年树，8崽分组持续斧击，按真实倒树数汇报；不是承诺任何环境均4棵。
- 原问题：每崽一次原生斧击，目标最多12个按崽轮分，所以8崽通常只给8棵树各一击。仅有效斧击不能代表树倒。
- 允许：ProjectileGroup/SwordResourceTargets 的有界连击原子及真实完成计数，TS选择次数/目标上限，Gateway/Skill工具可选能力接线、相关测试、报告与本轮产物。保持策略/参数由TS选择，Mod只做有上限逐帧执行和原生回执，不添加自动学习任务或无界砍树循环。
- 保持：单人、6格范围、普通成年树且未挂树液器/对象，使用背包现有斧头，单次体力消费、60秒继承时限、墙体碰撞、停/取消墓碑、幂等、4正4背单剑顺时针、首轮真实回复并播放完成后才判断/执行。树桩、果树和受保护树不自动清理，掉落仍由原版处理。
- 不改变：捕鱼三次原生抽取/无鱼不当成功、打怪伤害/解锁规则、爷爷、供应商/收费/账户/存档、其他游戏、共享打包热点。
- 测试先行：真实TS->真实ProjectileGroup->真实SwordResourceTargets，外部Tree/世界模拟有血量和倒树状态，不用固定Apply返回1证明倒树。先对本轮冻结旧源证明8击无倒树失败，再候选通过；新增捕鱼/战斗从已召唤组接续测试。检查实际注册Agent/工具/Gateway及取消/旧会话/Work相关回归。
- 部署前：产物哈希、所有活动加载路径、原生源码快照；不覆盖运行游戏/Harness。此前退出许可已消费，新的增量部署需新的退出窗口；不强杀未授权进程。
- 交付边界：代码、自动测试、已安装、真实语音/游戏分开说。未接CI硬门禁时明确尚无自动阻断。

当前状态（2026-09-13 02:29）：两份修复已整合到本任务源码、通过联合回归并增量安装，AG-harness已正常重启，真实游戏/真人语音尚未验收。以下保留9月12日部署冲突历史，不再代表当前阻塞。

## 用户要求更新后的部署前检查

- 用户后续说“更新把”。本次尚未执行install.mjs、尚未覆盖文件、尚未结束任何进程。
- 发现安装基线在候选准备后变化：当前插件归档为 `272a0ed68de569cd79e9e38639c9183bff0e403b80c12b600ef7428c14b3494f`，Steam DLL为 `ec17a03ec7a047d2357508ae21d30ebae664fd8762d03a69a1bef39057a78e44`，两者既不是预期旧版也不是本轮候选。文件时间分别为17:51、17:49。
- 已安装Gateway和SwordTools新增 `projectileStopMessage` 接线，来自 `runtime/context/projectile-live-state.js`。本候选不含此接线，直接部署会撤销该修改。当前工作区59项候选源码哈希未漂移，但其他安装修改来源/占用尚未确认，不能仅改安装清单的before哈希绕过检查。
- 34个目标中14个既非预期旧版也非候选；其余部分声明文件before/after相同，不能把after匹配数当部分安装成功。
- 最后检查AG-harness仍有5个进程（76056、70300、74664、75376、66532），星露谷未运行。不能仅凭进程数称其为残留，也不能使用旧轮结束进程许可。
- 暂停部署并向用户确认：是否暂停其他相关更新，由本任务整合两份修复；同时需要正常退出窗口。不擅自向其他任务发消息。

## 本轮证据

## 2026-09-13 用户授权本任务整合

- 用户明确说“那请整合把”：由当前任务合并已安装的实时状态/HUD/停止反馈与待装的砍树增强，重新回归后增量更新。仍不提交/推送，不跨任务派发消息或新建子代理。
- 已核实来源：`C:/game/deepseekharness/.artifacts/sword-screen-merged-20260912` 的真实源码、基线源码和验证记录；其DLL和已安装版均为ec17a03e...。不能把生成的JS当唯一源代码交接。
- 合并位置仍为本任务source-unified工作区；保留所有既有改动。先保存本任务前态，再三方合并screen基线、当前strength源码、screen候选；仅改涉及的生产/测试文件及当前claim/验收报告。
- 新不变量：保留HUD实时冷却、观察两入口、历史状态过期保护、急停回执的live.screenLabel；同时保留连续斧击预算、真实倒树数、召唤接续、顺时针4正4背、首轮正常回复/播放完成门控、Work与学习原有边界。
- 验证：先以合并后的同一测试套件在已安装screen原生实现运行倒树反证；再合并实现通过；联合screen原生33/控制器229与strength倒树/取消、TS真实Agent/Gateway/工具/上下文/语音/Work回归。外部游戏/模型为模拟，真实语音游戏未测；不变更供应商/账号/模型/存档，不扩充CI或正式安装包。
- 最新只读检查：AG-harness和星露谷进程均0。资源包仍为272a0ed6...；另外发现的dst-learning候选尚未安装，已装SkillTools与本轮之前基线哈希相同，不把未获本次授权的学习候选顺带发布。
- 部署前后比较全部Runtime/Profile JS、恢复包及Mod，保留app.asar中的现有退出修复，使用内容校验阻止中途其他版本改写。若再次检测到应用运行，不强杀，停止覆盖。

## 先前增强测试证据

### 9月13日整合交付结果

- 来源：screen候选的17个生产/测试文件按其记录的基线与本任务strength源码三方合并；全部无文本冲突，另人工检查状态回执、更新循环和预算/停止的组合。screen基线Program多了一段反证，移除该已记录测试段后SHA256精确匹配原始基线，未放宽校验。
- 未把其他DST学习候选顺带发布；其安装JS仍是旧基线。本轮未跨任务调度、未修改其他任务claim、未提交/推送。
- 当前已安装screen原生源码的同一倒树测试：2个目标断言失败、捕鱼/战斗2通过；旧版8击伤8棵倒0棵。合并后19个测试文件174通过、0失败、0跳过；33原生画面、229控制器/回环WebSocket、21作用范围通过；TS和Mod编译通过、Mod0警告0错误。
- 全套首次执行2例因旧隔离目录测试数据路径不存在失败，记录test-layout-failure.json；已通过显式AGH_PROJECTILE_STATE_FIXTURE接线并全量重跑。该环境错误不作为行为反证。测试仍是显式启用的原生集成，尚未接正式CI/打包硬门禁。
- 证据根：`C:/game/deepseekharness/output/sword-integrated-20260913`，重跑命令test-integrated.ps1；产物、测试、源码锁定见stage-cm4muA/manifest.json。
- 增量安装34文件全部匹配；其余编译JS、包内文件、app.asar/退出修复/供应商/语音/其他Mod文件保持。原生源对照已装screen版本，只有ProjectileGroup和SwordResourceTargets不同；爷爷图集及DLL内嵌一致。
- 插件SHA256 `3fc28d0135e883333d9129eb03795d456ef04eb845865645095e5c2b3c134b17`；DLL SHA256 `4ac7669b06460975a2f00a8adc2ff9a90259aff9f9f223fdb648fcfee24eb2b1`。备份installed-before-8gFIqq。
- 启动验证：主进程82628、Runtime66880；Web55857返回200，33145归属本安装Runtime，gateway.ping返回pong:true；活动Profile依赖切换至新哈希恢复归档，34目标和保护文件重新核验一致，无本次启动fatal。
- 当前应用有意保持运行供测试，星露谷未启动；本轮未结束任何既有进程。不要把运行状态的多进程数叫残留。真实退出验收未在本次新增，之前正常退出证据仍保留，退出实现未变。
- 没有重制/发布完整安装器或更新旧朋友安装包；只完成当前源码和本机增量交付。报告：docs/testing/SWORD_INTEGRATED_20260913.md。

- 旧实现真实反证：`C:/game/deepseekharness/output/sword-chop-strength-20260912/old-strength.json`，2通过/2失败；失败是目标倒树断言，不是编译或依赖错误。8棵健康树固定测试世界中旧版8击、伤8棵、倒0棵；单树重复计数用例也失败。
- 候选：同测试世界48次原生斧击、触及4棵、实际倒4棵；背包斧头威力未改。高血量测试96击达到上限仍倒0棵时返回未完成；取消后无后续斧击。
- `final-tests.json`：132通过、0失败、0跳过，涵盖真实Agent/工具注册/Gateway/播放结束后判定/原生执行、召唤后捕鱼和战斗、语音取消/Work/学习相关边界。外部模型和游戏世界为模拟，不替代真实语音游戏验收。
- 原生控制器229项、作用范围21项、视觉26项通过；Mod编译0警告0错误，TS编译通过。
- 增量候选 `stage-dchG2B/manifest.json`：34个替换目标、59个源码/资源哈希。除允许的4个模块，全部已安装编译JS与候选一致；归档其他文件、供应商插件、SpeechController、app.asar、其他Mod文件均受保护。
- 53个原生C#文件与爷爷素材快照比较：差异仅上轮4文件加本轮SwordResourceTargets。上轮Client/Geometry/Renderer哈希不变，候选DLL嵌入爷爷图集哈希正确。
- 说明：此前漏验把“有效斧击”当用户的“树倒”；新回归必须检查健康值变化、真实倒树状态及不重复计数。原生集成测试仍需显式设置AGH_PROJECTILE_BRIDGE，本轮未建立全仓CI/打包硬性阻断，不宣称默认测试自动覆盖所有情况。
- 报告：`docs/testing/SWORD_CHOP_STRENGTH_20260912.md`。无提交/推送，无其他任务调度。
