# 幽灵爷爷浇地：小尺寸实现

## 最新安装检查（2026-09-08）

已使用批准立绘生成真实透明四格 atlas，并嵌入 Mod DLL，替换旧的矩形绘制角色。包含爷爷待机/倾碗、门关闭/打开（门内汤圆孟婆），实时水流从碗沿连接目标。动画为两姿态和门状态过渡，非完整逐帧手绘。此节覆盖下方历史状态。

已按用户授权关闭应用并局部更新本机：Steam StardewAgentMod.dll、当前官方 Profile 的四个 JS 文件、内置插件 tgz。保留旧包其他功能；未修改账号、密钥、存档、自配服务 Profile、app.asar 或共享发行源码。应用重新启动后 33145、DSH Web、产品 bridge 均就绪。

备份：`.artifacts/grandpa-install-20260908/deployment-20260908-174222/mapping.json`；六个安装文件 SHA256 均与源一致。安装版 DLL SHA256：`AB946E7FD00A5089C114F91C4F3893FB355E0E733631D4D279BA048DA1DECFEC`。

验证：插件 244/244；控制检查 218；Mod 编译零警告/错误；安装模块 import/语音收回匹配通过。未进入真实存档，未验证麦克风、GPU实际渲染及土壤变化。测试口令：“爷爷，帮忙浇水”；停止：“爷爷，回去吧”。只留下正常应用/Runtime。

## 幽灵门跟进

新增原生像素门框、开门/关门、门内小桥与孟婆、碗上“孟”字，水流起点移到碗沿。门为 96×116 像素，浇水期间保持打开；爷爷开门后出现，正常完成时回门淡出。此节替代下方旧版小桥淡出方案。

本次为原生图形近似，不是批准的高清立绘贴图接入；角色美术精修、碗的倾斜动画、实机遮挡与字体可读性仍待完成。编译零警告/错误，218 项控制检查通过；未安装，未验收渲染。共享热点未修改，未留下新增后台服务。

## 当前状态

源码已实现；未安装、未进行真实游戏或麦克风验收。概念图没有直接作为游戏贴图使用，当前以原小汤圆纹理和像素图形绘制。

## 交互与效果

- 说“爷爷浇地”：召唤幽灵爷爷，身后短暂出现约 144 像素（2.25 格）宽的小桥、孟婆、汤锅、爱心和短标签。
- 小桥约 1.5 秒内淡出；爷爷用紫青色汤水浇附近干地，正常结束时小桥重现，爷爷返回并消失。
- 说“爷爷回去吧”“爷爷歇会儿”或“停止浇地”：识别后走优先取消，不等待普通模型任务。取消直接清除，不播放正常结束的返回动画。
- 不生成永久桥梁、不占地图格子、不切换场景；实际地图修改只有确认后的干地浇水。

## 范围与约束

- 单人模式，玩家附近 6 格内，最多 12 块干地；不包含花盆或全地图。
- 每块间隔约 350 毫秒；只统计仍是同一块干地且实际变为湿地的结果。
- 消耗 1 点小精灵体力，沿用 8 秒冷却及菜单、事件、死亡、换地图、断连保护。
- 与剑阵共用一个有界效果组，不能同时运行。收回指令取消当前效果组。
- 无作物遗忘或其他“孟婆汤”副作用。

## 自动检查

- 插件构建及测试：244/244 通过。
- 原生控制器、游戏替身及真实 loopback WebSocket：218 项通过。
- Stardew Mod 编译：0 警告、0 错误。
- 上述检查不验证真实渲染；需实机检查小字体、遮挡、方向、汤水落点和语音中断延迟。

## 实机验收

安装对应插件及 Mod 后，在存档附近准备数块干地，说“爷爷浇地”。检查召唤小景足够小、爷爷仍像小汤圆、干地确实湿润、完成数正确及返回消失。另测中途停止、无干地和换地图，不得残留或继续浇水。

本轮未替换正在运行的 Desktop，未提交或推送代码，未修改共享发行配置。
