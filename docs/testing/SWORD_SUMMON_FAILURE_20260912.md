# 万剑归宗未生效：本机实际记录

> 后续状态：用户授权修复及退出后增量更新，已于 2026-09-12 执行。修复、回归和实际安装证据见 `SWORD_VOICE_REPAIR_20260912.md`。本文下述“尚未修复”描述的是当时诊断阶段，保留作历史依据。

## 结论与边界

2026-09-12 用户反馈召唤未生效。最新游戏运行记录为当日 02:02:17–02:04:23（北京时间）。本轮证据表明请求没有进入召唤绘制：第一轮送到模型的文本是“归中。”，执行了收回；后两轮在 ASR 识别失败。不能把这些记录当作四正四背的视觉实现失败，也不能反过来宣称视觉已通过实机验收。

当前只完成诊断，尚未修复语音故障。用户没有再次启动游戏验收；本任务没有停止应用、覆盖运行时、修改配置/密钥/存档或提交代码。

## 时间线

| 北京时间 | 实际证据 | 能证明什么 |
| --- | --- | --- |
| 02:02:24 | SMAPI 从 `Mods/StardewAgentMod/StardewAgentMod.dll` 加载 0.8.2 | Mod 被加载；当前文件 SHA256 仍为 `EF0CE12A6A3418824C9847A2C57AAAA376BF376B5B3C064170C1AC400D1AEFEE` |
| 02:03:00 | 同伴素材加载，测试模式临时解锁日志 | 素材与解锁入口存在；不证明已调用召唤 |
| 02:03:11–13 | V 键录音开始/停止 | 录音流程被触发；未保留原始录音，不能判定实际说出的完整内容或丢字环节 |
| 02:03:16.967 | 真实 Session turn 15 / seq 799：`Player message: 归中。` | 模型实际收到的不是完整“万剑归宗” |
| 02:03:24.231–24.295 | seq 813–815：模型说“剑阵冷却中，先归中收回～”，调用 `xiaotangyuan_sword_formation_stop`，收到“已确认没有活动中的崽崽剑阵。” | 此轮没有调用 summon/create；“冷却中”没有本轮查询/创建回执支持 |
| 02:03:27.714 | seq 825：“剑阵已归中～” | 模型按停止意图结束；不是召唤成功 |
| 02:03:37–47 | 新录音流式完成失败，再退回整段识别亦失败，错误 `e62fdb24` | ASR HTTP 500；录音完成后到最终失败约 8.22 秒 |
| 02:03:54–02:04:04 | 第三轮同样两次失败，错误 `578d75dd` | ASR HTTP 500；约 8.48 秒；未进入模型召唤 |
| 02:04:23 | `Disconnected: ClosedGame` | 游戏已退出，不擅自认定崩溃 |

第一轮在 02:03:37 被下一次录音打断语音播放；该 `voice.cancelled` 是打断记录，不能当作识别失败根因。

## 500 实际来自哪里

- 当前官方托管插件配置为 disabled、本地提供商模式 enabled，日志 provider 是 `minimax-local`，ASR 使用 `iflytek`。
- 已读安装版 `app.asar` 中的 `commercial-main.mjs`、`local-provider-handler.mjs`：本地桥接调用讯飞传输模块，通用异常由 `errorPayload` 映射为 HTTP 500。
- 已装 `resources/runtime/plugins/minimax-iflytek/index.mjs:35` 遇到非成功 HTTP 状态直接抛出 `语音识别失败 HTTP ${response.status}`，没有读出响应中的错误码/消息；因此持久错误日志只剩 500。
- 不能由此推断为余额不足、Key 无效、讯飞接口改版或服务器永久不可用。也不能把“配置齐全”当作服务可用。
- 本轮仅作一次诊断请求：200ms 人工静音，通过与安装版逐字一致的本地 handler 和实际已装传输模块返回成功（645ms、文本长度 0）。没有使用或保存玩家录音，没有调用模型或游戏，没有修改凭据。未重现历史 500，不证明真实 ASR 已恢复或没有丢词。

另有启动时 `adapter.hello must be sent before assistant.compose`，影响一次晨间自动发言；它与之后两个已明确定位在 ASR 的失败分开记录，不将其混为召唤直接原因。

## 为什么上轮检查没有发现

- 上轮 25 项绘制检查覆盖真实生产渲染函数，但游戏/GPU 使用替身；226 项检查覆盖控制器及本地回环 RPC。这些检查没有输入真实麦克风、讯飞流式消息或恢复后的真实模型会话。
- 本轮失败恰好在这些检查之前：识别文本不足 / 请求识别失败。安装哈希一致只能排除当前 DLL 被换回旧版，不能保证语音到动作的闭环。
- 当前模型收到不完整文本后又缺少真实状态依据便声称“冷却中”，也是独立的执行判断/汇报问题，不能通过再次更换视觉 DLL 修复。

## 下一步应修与验收

1. 保留脱敏 ASR 错误类别、上游错误码、阶段、关联 ID；客户端与本地桥接均不得抹去原因，不能写出密钥、签名 URL、原始音频。对不同错误分类处理降级，避免同一错误盲目重复请求。
2. 用确定音频/可控分段回执核验开始、分段合并、结束全过程；先让回归抓住旧行为。当前没有原音频，不能断言“归中。”必然是某个特定丢帧/合并 bug，更不能硬编码把所有“归中”当成召唤。
3. 在真实 Agent 建立/旧会话恢复链路模拟外部模型、语音和游戏，覆盖完整召唤、相似词、收回、否定、已有技能、普通陪聊、Work 和取消。执行必须符合 `REPLY_FIRST_ACCEPTANCE.md` 的回复后边界；冷却、成功、停止等状态必须有本轮真实回执。
4. 后续实现需单独确认语音/Agent/提供商模块的认领与安装窗口；不可直接覆盖别的任务未整合改动。测试候选和 Runtime/Profile/恢复包逐项对应后再热更新，最后做真实语音召唤。这里没有新增 CI/安装强制门禁。

## 原始证据位置与读取口径

- `C:/Users/10354/AppData/Roaming/StardewValley/ErrorLogs/SMAPI-latest.txt`。
- `C:/Users/10354/AppData/Roaming/AI Native Game Harness 商业版/logs/runtime.log`：786–849 行相关错误/诊断；不输出无关学习/观察快照。
- 同 Profile 的 `dsh-home/diagnostics/errors-2026-09-11.jsonl`：3–6 行为两轮流式/降级识别失败。
- 同 Profile 的 `dsh-home/sessions/.../game-stardew-valley-b53135b2178cef9371c96508/session.jsonl.zstd`：seq 799、813–815、825。该文件是多帧 Zstd，单次 one-shot 解压只返回首帧 header；本轮按每帧 `engine.bytesWritten` 推进读取了全部 581 条记录，没有改写 Session。
- 源码/安装文件只读核验及人工静音探测均已退出。未创建子代理、未联系其他任务；保留用户已运行的 Harness。
